name: Firmware CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  firmware:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements-ci.txt

      - name: Cache PlatformIO packages
        uses: actions/cache@v4
        with:
          path: ~/.platformio
          key: ${{ runner.os }}-pio-${{ hashFiles('requirements-ci.txt', 'platformio.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-

      - name: Install PlatformIO
        run: python -m pip install --requirement requirements-ci.txt

      - name: Show PlatformIO version
        run: pio --version

      - name: Check repository safety
        run: python scripts/check_repo_safety.py

      - name: Run native tests
        run: pio test -e native

      - name: Build production firmware
        run: pio run -e esp32-c3

      - name: Create placeholder-only smoke compile configuration
        run: cp include/interbridge_dev_secrets.example.h include/interbridge_dev_secrets.h

      - name: Verify smoke configuration remains ignored
        run: |
          git check-ignore include/interbridge_dev_secrets.h
          test -z "$(git status --short --untracked-files=all)"

      - name: Compile DEV MQTT smoke firmware
        run: pio run -e esp32-c3-dev-mqtt

      - name: Compile DEV physical ring simulator firmware
        run: pio run -e esp32-c3-dev-ring-simulator

      - name: Compile isolated DEV BLE provisioning firmware
        run: |
          cp include/interbridge_ble_dev_secrets.example.h include/interbridge_ble_dev_secrets.h
          pio run -e esp32-c3-dev-ble-provisioning

      - name: Compile Phase 3C.4 DEV BLE+MQTT composed firmware
        run: |
          cp include/interbridge_dev_ble_mqtt_secrets.example.h include/interbridge_dev_ble_mqtt_secrets.h
          pio run -e esp32-c3-dev-ble-mqtt

      - name: Compile Si3050 clock probe generator (ESP32-C3)
        run: pio run -e esp32-c3-si3050-clock-probe

      - name: Compile Si3050 clock probe meter (ESP32 DevKitV1)
        run: pio run -e esp32dev-si3050-clock-meter
