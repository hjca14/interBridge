#pragma once

// Placeholder used only to prove that the isolated BLE target compiles.
// Copy this file to interbridge_ble_dev_secrets.h and replace the value
// locally before bench use. Never commit the copied file or a real PoP.
#define INTERBRIDGE_DEV_BLE_POP "REPLACE_WITH_LOCAL_DEV_POP"
